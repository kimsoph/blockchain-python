# md-cleaner v3.1 dependencies
# 마크다운 클리닝 스킬 의존성 패키지

# 필수 의존성
chardet>=5.0.0  # 인코딩 자동 감지 (UTF-8 외 파일 처리)

# 선택적 의존성 (설치 시 추가 기능 활성화)
# tqdm>=4.60.0  # 배치 처리 시 진행률 표시 (선택)
# wcwidth>=0.2.0  # 테이블 정렬 시 한글 문자 너비 계산 (선택)
