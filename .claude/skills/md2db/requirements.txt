# md2db v2.0.0 의존성

# 필수 의존성
PyYAML>=6.0

# 선택적 의존성 (진행률 표시)
tqdm>=4.65.0

# ChromaDB 기능 사용 시 (선택적)
# pip install chromadb sentence-transformers
# chromadb>=0.4.0
# sentence-transformers>=2.2.0

# 개발/테스트용
# pytest>=7.0.0
# pytest-cov>=4.0.0
